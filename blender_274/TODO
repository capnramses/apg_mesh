* actually get length of each vector found boundrad, not just biggest component
(this is a bounding box extent, not a radius)
* normals
* UVs
* tangents
* skeleton
* animations
* export meshes from all objects, not just a single, selected object
